#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

"$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/Users/xuwei/projects/zzxx/videograb/frontend/node_modules/.pnpm/vite@5.0.11/node_modules/esbuild/bin/esbuild
